// TranscriptViewer.jsx
// Purpose: VTT transcript panel with word-sync highlighting synced to the active video playhead

const TranscriptViewer = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">TranscriptViewer</h1>
      <p className="text-gray-500 mt-2">VTT transcript panel with word-sync highlighting synced to the active video playhead</p>
    </div>
  );
};

export default TranscriptViewer;
