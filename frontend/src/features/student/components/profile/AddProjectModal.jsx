// AddProjectModal.jsx
// Purpose: Modal dialog form for adding a new portfolio project with title, URL, and description fields

const AddProjectModal = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">AddProjectModal</h1>
      <p className="text-gray-500 mt-2">Modal dialog form for adding a new portfolio project with title, URL, and description fields</p>
    </div>
  );
};

export default AddProjectModal;
