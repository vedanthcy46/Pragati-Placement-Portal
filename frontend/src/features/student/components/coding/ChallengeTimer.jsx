// ChallengeTimer.jsx
// Purpose: Horizontal countdown bar showing remaining time until the coding challenge window closes

const ChallengeTimer = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">ChallengeTimer</h1>
      <p className="text-gray-500 mt-2">Horizontal countdown bar showing remaining time until the coding challenge window closes</p>
    </div>
  );
};

export default ChallengeTimer;
