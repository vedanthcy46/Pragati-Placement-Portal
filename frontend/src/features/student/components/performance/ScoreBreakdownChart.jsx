// ScoreBreakdownChart.jsx
// Purpose: Bar chart comparing the student's scores per activity type against the batch average

const ScoreBreakdownChart = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">ScoreBreakdownChart</h1>
      <p className="text-gray-500 mt-2">Bar chart comparing the student's scores per activity type against the batch average</p>
    </div>
  );
};

export default ScoreBreakdownChart;
