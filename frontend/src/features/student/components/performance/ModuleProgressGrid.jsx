// ModuleProgressGrid.jsx
// Purpose: Grid layout displaying a completion ring for each course module the student is enrolled in

const ModuleProgressGrid = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">ModuleProgressGrid</h1>
      <p className="text-gray-500 mt-2">Grid layout displaying a completion ring for each course module the student is enrolled in</p>
    </div>
  );
};

export default ModuleProgressGrid;
