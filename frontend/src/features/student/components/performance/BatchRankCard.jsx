// BatchRankCard.jsx
// Purpose: Displays the student's current batch rank and percentile without revealing raw score values

const BatchRankCard = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">BatchRankCard</h1>
      <p className="text-gray-500 mt-2">Displays the student's current batch rank and percentile without revealing raw score values</p>
    </div>
  );
};

export default BatchRankCard;
