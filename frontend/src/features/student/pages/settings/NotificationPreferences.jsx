// NotificationPreferences.jsx
// Purpose: Settings page with per-notification-type channel toggles for email, SMS, and in-app alerts (SM-12)

const NotificationPreferences = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">NotificationPreferences</h1>
      <p className="text-gray-500 mt-2">Settings page with per-notification-type channel toggles for email, SMS, and in-app alerts (SM-12)</p>
    </div>
  );
};

export default NotificationPreferences;
