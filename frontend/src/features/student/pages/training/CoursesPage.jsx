// CoursesPage.jsx
// Purpose: Enrolled course listing page with progress rings for each course module (SM-04)

const CoursesPage = () => {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-gray-800">CoursesPage</h1>
      <p className="text-gray-500 mt-2">Enrolled course listing page with progress rings for each course module (SM-04)</p>
    </div>
  );
};

export default CoursesPage;
